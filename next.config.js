/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;
// next.config.ts is the active config — this file is intentionally empty.
// Having both .js and .ts causes conflicts; keep this as a no-op placeholder.
module.exports = {};
